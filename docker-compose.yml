version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - uploads_data:/app/datatalk/data/uploads
    env_file:
      - .env
    environment:
      - APP_ENV=development
    restart: unless-stopped

volumes:
  uploads_data:
